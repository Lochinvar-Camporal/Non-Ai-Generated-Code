// write a function named is_even that: takes a i32 returns true if the number is even, and false if not
fn is_even(x: i32) -> bool { 
    let myMod = x % 2; 
    if ( myMod == 0 ) { 
            true } 
        else { 
            false } 

} // is this good
